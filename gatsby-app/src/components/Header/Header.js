import React from "react";
import Logo from "./Logo";
// Styles
import "./Header.css";

function Header({}) {
  return (
    <header className="o-header">
      <Logo />
    </header>
  );
}

export default Header;
